# Nested Skill Directories

The codebase contains nested skill directories (e.g., `scientific-skills/skills/*`). Validation tools and scripts that expect a flat `.claude/skills/{name}/SKILL.md` directory structure must be updated to explicitly support these nested paths.
