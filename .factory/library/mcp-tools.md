# MCP Tools Data Structure

MCP tools are defined in `.claude/config/tool-manifest.json`.
When interacting with or filtering MCP tools, note that each MCP tool object includes an `mcp_server` property that specifies which server the tool belongs to. This is essential for features like `mcpServers` scoping.
