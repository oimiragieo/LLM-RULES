{
  "groupId": "windows-cleanup-all",
  "testedAt": "2026-03-29T10:55:00.000Z",
  "isolation": {
    "directory": "C:\\dev\\projects\\agent-studio"
  },
  "toolsUsed": ["Execute"],
  "assertions": [
    {
      "id": "VAL-WP-001",
      "status": "pass",
      "steps": [
        {
          "action": "Run pnpm validate:full:parallel",
          "expected": "Exit code 0",
          "observed": "Exit code 0"
        }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-001-validate-full-parallel.txt"]
      }
    },
    {
      "id": "VAL-WP-002",
      "status": "pass",
      "steps": [
        { "action": "Run pnpm test:tools", "expected": "Exit code 0", "observed": "Exit code 0" }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-002-test-tools.txt"]
      }
    },
    {
      "id": "VAL-WP-003",
      "status": "fail",
      "reason": "output failed (SyntaxError in pnpm inside WSL bash due to ?. token support issue)",
      "steps": [
        {
          "action": "Run bash .factory/init.sh",
          "expected": "Exit code 0",
          "observed": "Exit code 1"
        }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-003-init-sh.txt"]
      },
      "issues": "init.sh relies on WSL bash executing node/pnpm. The WSL environment had an older node version causing a SyntaxError on '?.' in pnpm."
    },
    {
      "id": "VAL-WP-004",
      "status": "pass",
      "steps": [
        {
          "action": "Run pnpm index-rules && git diff --stat",
          "expected": "0 changes to generated files",
          "observed": "0 changes to generated files"
        }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-004-index-rules.txt"]
      }
    },
    {
      "id": "VAL-WP-005",
      "status": "fail",
      "reason": "format:check failed on regenerated file rule-index.json",
      "steps": [
        { "action": "Run pnpm format:check", "expected": "Exit code 0", "observed": "Exit code 1" }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-005-format-check.txt"]
      },
      "issues": ".claude/context/config/rule-index.json has code style issues after generation, meaning format:check did not pass."
    },
    {
      "id": "VAL-WP-006",
      "status": "pass",
      "steps": [
        {
          "action": "Run test on tokens.md",
          "expected": "Exit code 0 and file exists",
          "observed": "Exit code 0"
        }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-006-tokens-command.txt"]
      }
    },
    {
      "id": "VAL-WP-007",
      "status": "pass",
      "steps": [
        { "action": "Run pnpm validate:full", "expected": "Exit code 0", "observed": "Exit code 0" }
      ],
      "evidence": {
        "terminal": ["windows-cleanup/windows-cleanup-all/VAL-WP-007-validate-full.txt"]
      }
    }
  ],
  "frictions": [
    {
      "description": "bash .factory/init.sh fails in WSL environment due to WSL's node.js installation (or resolving Windows pnpm bin inside WSL which fails with syntax error on `?.` token).",
      "resolved": false,
      "affectedAssertions": ["VAL-WP-003"]
    },
    {
      "description": "cmd /c needed for executing `pnpm index-rules && git diff --stat` because `&&` is not supported in the default PowerShell runner.",
      "resolved": true,
      "resolution": "Used cmd /c to execute the sequence.",
      "affectedAssertions": ["VAL-WP-004"]
    }
  ],
  "blockers": [],
  "summary": "Tested 7 assertions: 5 passed, 2 failed (VAL-WP-003 WSL bash init.sh failed, VAL-WP-005 format:check failed on rule-index.json)."
}
