interface:
  display_name: Chrome Browser
  short_description: Browser automation with two integrations - Chrome DevTools MCP
  default_prompt: Use the $chrome-browser skill to complete this task safely and efficiently.
dependencies:
  tools:
    - type: mcp
      value: chrome-devtools
    - type: mcp
      value: claude-in-chrome
