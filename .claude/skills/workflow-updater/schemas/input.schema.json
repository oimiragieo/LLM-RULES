{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "workflow-updater input",
  "type": "object",
  "required": ["workflow"],
  "properties": {
    "workflow": { "type": "string" },
    "trigger": { "type": "string", "enum": ["manual", "reflection", "evolve"] }
  }
}
