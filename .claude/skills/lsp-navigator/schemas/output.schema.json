{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "lspNavigatorOutput",
  "description": "Output from LSP navigation operations.",
  "type": "object",
  "properties": {
    "ok": {
      "type": "boolean",
      "description": "Whether the operation succeeded."
    },
    "operation": {
      "type": "string",
      "description": "The LSP operation that was invoked."
    },
    "results": {
      "type": "array",
      "description": "LSP operation results. Structure depends on the operation.",
      "items": {
        "type": "object",
        "description": "A single LSP result entry (location, symbol, hover text, etc.)"
      }
    },
    "summary": {
      "type": "string",
      "description": "Human-readable summary of the operation result."
    },
    "error": {
      "type": "string",
      "description": "Error message if the operation failed."
    }
  },
  "required": ["ok"],
  "additionalProperties": false
}
