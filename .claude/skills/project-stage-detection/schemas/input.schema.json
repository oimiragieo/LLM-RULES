{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "ProjectStageDetection Input",
  "description": "Validated inputs for project-stage-detection skill execution",
  "type": "object",
  "properties": {
    "targetDir": {
      "type": "string",
      "description": "Absolute or relative path to the project root to analyze. Defaults to current working directory."
    },
    "outputFormat": {
      "type": "string",
      "enum": ["json", "markdown", "both"],
      "default": "json",
      "description": "Format for the detection output"
    },
    "stageOverride": {
      "type": "string",
      "enum": ["new", "early", "mid", "mature"],
      "description": "Override detected stage for testing or manual correction"
    },
    "skipCache": {
      "type": "boolean",
      "default": false,
      "description": "Force re-detection even if a recent cached result exists in learnings.md"
    }
  },
  "additionalProperties": false
}
