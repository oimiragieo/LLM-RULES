# UI vs Reflection Review (Summary)

- Make Step 0 explicit to users (start + complete).
- Keep reflection first-class in pipeline updates.
- Reduce noise by batching late background completions.
- Always include reflection report path in outcome summaries.
- Prefer source-level stale path normalization over guardrail-only fallbacks.
