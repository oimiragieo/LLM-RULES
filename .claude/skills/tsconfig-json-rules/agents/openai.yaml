interface:
  display_name: Tsconfig Json Rules
  short_description: Defines general rules for tsconfig.json. It suggest using
  default_prompt: Use the $tsconfig-json-rules skill to complete this task safely and efficiently.
