# hooks-explainer Commands

## Primary Invocation

- `/hooks-explainer --topic overview`
- `/hooks-explainer --topic protected_paths`
- `/hooks-explainer --topic bypasses`
- `/hooks-explainer --topic taskupdate`

## CLI Invocation

```bash
node .claude/skills/hooks-explainer/scripts/main.cjs --topic protected_paths
node .claude/skills/hooks-explainer/scripts/main.cjs --topic taskupdate --json
```
