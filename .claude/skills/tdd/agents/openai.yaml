interface:
  display_name: Tdd
  short_description: Canon TDD for humans and AI agents (scenario backlog, RED proof, minimal GREEN, optional refactor).
  default_prompt: Use the $tdd skill and provide RED/GREEN evidence for each scenario.
