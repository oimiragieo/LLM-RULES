{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ecosystem-integrity-scannerInput",
  "description": "Input schema for deeply analyzes framework structural health, catching phantom references, wrong require() depth paths, missing dependencies inside agents and hooks, bloated configurations, and empty tool directories.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "target": {
      "type": "string",
      "description": "Target file or path for the skill to operate on"
    },
    "options": {
      "type": "object",
      "description": "Additional options for skill execution",
      "additionalProperties": true
    }
  }
}
