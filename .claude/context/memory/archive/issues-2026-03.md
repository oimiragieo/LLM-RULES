# issues Archive (2026-03)

## ISSUE: CLAUDE.md Bash/Gap-Observation Protocol Contradiction (2026-03-01)

**Status:** PARTIALLY RESOLVED — CLAUDE.md updated to whitelist gap-log Bash append, but formal ADR needed

**Finding:** Gemini external review identified a critical contradiction:

- Section 0 (Banned Tools): "Router may NEVER use Bash... (EXCEPT read-only git status/log)"
- Gap Observation Protocol: "Router MUST append... using Bash: `echo '...' >> session-gap-log.jsonl`"

**Impact:** Router would experience "logic lock" when commanded to log a gap but forbidden from using Bash.

**Current State:** CLAUDE.md line 37 updated to add "gap-log append" to the Bash exception list. However:

1. `router-tool-lockdown.cjs` may still block the Bash gap-log append unless updated
2. No formal ADR documenting the resolution decision
3. Consider implementing a dedicated LogEvent tool or TaskUpdate-based logging as permanent fix

**Source:** Gemini 8.5/10 external review — `.claude/context/reports/backend/claude-md-gemini-review-2026-03-01.md`

---

## ISSUE: Recurring Missing TaskUpdate Summary Metadata (2026-03-01)

**Status:** OPEN — Pattern recurring across sessions

**Observed:** 2 of 4 tasks in the 2026-03-01 session completed with only the fallback string "Task N completed without summary metadata". Reflection system cannot learn from these completions (score withheld per Iron Law).

**Tasks affected:** Task 1 (18:17 UTC), Task 2 (18:40 UTC)

**Impact:**

- Reflection quality degrades to 0% for affected tasks
- Learning system accumulates `insufficient_data` entries
- Router/architect cannot extract patterns from completed work

**Remediation:**

- `pre-completion-validation.cjs` should hard-block completion when `metadata.summary` equals the fallback pattern
- Consider adding test: `expect(summary).not.toMatch(/^Task \d+ completed without summary metadata$/)`

---

## CRITICAL DIAGNOSIS: Reflection-Agent Atomic Handshake Root Cause (2026-02-22 Task #37)

**Status**: DIAGNOSED — Root cause identified, fix strategy documented

**Root Cause**: Background task spawning (`run_in_background: true`) does not properly initialize tool whitelist for reflection-agent. When Router spawns reflection-agent in background mode, the TaskUpdate tool becomes unavailable at runtime despite being listed in frontmatter.

**Evidence**:

- session-gap-log.jsonl entry (2026-02-22T01:30:00Z): "Background-spawned reflection-agent (run_in_background:true) reported TaskUpdate unavailable"
- Entry context: "Root cause: run_in_background spawns may not receive full tool whitelist"
- Mitigation note: "never spawn reflection-agent with run_in_background:true — always foreground"

**Affected Components**:

1. **tool-scope-validator.cjs** — may not handle background vs foreground context correctly
2. **pre-completion-validation.cjs** — may block metadata-only TaskUpdate calls from reflection-agent
3. **reflection-step0-guard.cjs** — cannot clean up processed requests without atomic handshake

**Fix Strategy**:

1. Audit tool-scope-validator.cjs line-by-line for background context handling
2. Test pre-completion-validation.cjs with reflection-agent metadata payload
3. Add foreground-only enforcement to CLAUDE.md Step 0 routing-guard.cjs
4. Create CI test that verifies reflection-agent atomic handshake in isolation

**Analysis Report**: `.claude/context/reports/reflections/reflection-agent-atomic-handshake-analysis-2026-02-22.md` (200+ lines, full diagnosis with hypotheses and verification steps)

---

## ISSUE: Reflection-Agent Cannot Complete Atomic Handshake (2026-02-22 BLOCKER)

**Status**: OPEN — P1 BLOCKER

**Observed**: Reflection-agent invoked to process tasks 21, 22, 14 but cannot call TaskUpdate() for atomic completion handshake.

**Error**: "No such tool available: TaskUpdate" when attempting to mark reflection complete with processedReflectionIds metadata.

**Impact**:

- Reflection-spawn-request.json entries remain marked as processed: false
- reflection-cleanup.cjs cannot remove processed reflections
- Next Router iteration sees same reflections again (duplicate processing)
- Memory state becomes inconsistent

**Expected Behavior** (per CLAUDE.md Section 2):

```javascript
TaskUpdate({
  taskId: 'reflection-task-X',
  status: 'completed',
  metadata: { processedReflectionIds: ['task_completion:...', 'task_completion:...'] },
});
```

**Actual Behavior**: TaskUpdate tool not available in reflection-agent runtime context.

**Root Cause**: Possible causes:

1. Reflection-agent spawned via Skill() (non-standard routing) instead of Task()

---

## ISSUE: TaskUpdate Metadata Contract Violation — Batch (2026-03-01)

**Status**: ACTIVE — Pattern detected in multiple tasks

**Pattern**: All 5 tasks (7-11) from git commit pipeline completed without required metadata:

- `summary` field: missing (fallback string "Task X completed without summary metadata")
- `filesModified` array: missing
- `outputArtifacts` array: missing

**Impact**:

- Reflection agent cannot validate completion quality (PHASE 0 data sufficiency gate activated)
- Router cannot track file dependencies across tasks
- Audit trail incomplete — no evidence of what changed
- Post-completion integration cannot auto-detect artifacts

**Root Cause**: pre-completion-validation.cjs hook likely in warn-mode (non-blocking) or disabled

**Required Fix**:

- Verify hook is enabled in `.claude/settings.json`
- Escalate to BLOCK mode (currently may be warn-only)
- Increase spawn template warning prominence
- Add explicit metadata field checklist to universal-agent-spawn.md

**Occurrence**: 100% of sampled tasks in this batch (5/5) — systemic pattern

---

## ISSUE: 6 Critical Bugs in Evolution/Reflection Subsystems (2026-03-03)

**Status**: OPEN — Identified by multi-LLM review (Codex + Gemini), Task #4

**Source**: Multi-LLM review of 8 framework files: unified-reflection-handler.cjs, reflection-step0-guard.cjs, evolution state schema/guard/sync, loop-state-manager.cjs, unified-reflection-events.cjs, reflection-spawn-request.json queue

### Bug 1: I/O Bottleneck in generateAndPersistDispatchPlan (CRITICAL — P0)

- **File**: `.claude/hooks/reflection/unified-reflection-handler.cjs`
- **Issue**: `generateAndPersistDispatchPlan()` runs on EVERY PostToolUse invocation, causing escalating latency
- **Impact**: Reflection spawn latency grows with hook invocation frequency
- **Fix**: Gate behind condition — only invoke when new dispatch plan is actually needed (e.g., when reflection queue is non-empty or reflection IDs have changed)

### Bug 2: Timestamp Path Mismatch in trimOldReflections (CRITICAL — P0)

- **File**: `.claude/hooks/reflection/reflection-step0-guard.cjs`
- **Issue**: `trimOldReflections()` uses incorrect timestamp path or field comparison, silently clears entire reflection queue instead of only stale entries
- **Impact**: Valid pending reflections dropped before processing — data loss
- **Fix**: Verify timestamp field name matches schema; add test that confirms queue retained after trimOldReflections on fresh entries

### Bug 3: Evolution State Naming Inconsistency (HIGH — P1)

- **Files**: Evolution guard, evolution schema (gerunds: `evolving`, `reviewing`) vs sync module (verbs: `evolve`, `review`)
- **Issue**: Field naming inconsistency between guard/schema and sync module causes silent state deserialization failures
- **Impact**: Evolution state transitions may silently fail without error
- **Fix**: Standardize all evolution state names — pick gerunds OR verbs, update all 3 files atomically

### Bug 4: Passive Circuit Breaker — No checkAndBlock() (HIGH — P1)

- **File**: `.claude/lib/self-healing/loop-state-manager.cjs`
- **Issue**: `loop-state-manager.cjs` is a passive observer — has no `checkAndBlock()` method; cannot actually prevent runaway loops
- **Impact**: Self-healing circuit breaker is non-functional; escalation thresholds defined but never enforced
- **Fix**: Add `checkAndBlock(agentType, context)` method; call from reflection-handler before spawning new reflection agents

### Bug 5: Memory Poisoning Risk via Regex Extraction (HIGH — P1)

- **File**: `.claude/hooks/reflection/unified-reflection-events.cjs`
- **Issue**: Regex-based pattern extraction on agent output strings can be manipulated by adversarial agent output; no sanitization before writing to patterns.json
- **Impact**: Memory poisoning (ASI06 — OWASP Agentic AI Top 10) — malicious patterns injected into persistent memory
- **Fix**: Apply `memory-sanitizer.cjs` to all extractions; use schema-validated MemoryRecord API only; never write raw agent output directly to memory

### Bug 6: Queue Overflow ID Guard Bug (MEDIUM — P2)

- **File**: `.claude/context/runtime/reflection-spawn-request.json` handler
- **Issue**: ID guard logic drops valid requests when reflection IDs are `undefined` or `null` (falsy) rather than checking length or schema validity
- **Impact**: Valid reflections silently dropped when ID generation fails
- **Fix**: Replace falsy guard `if (!id)` with strict schema check `if (typeof id !== 'string' || id.length === 0)`

---

## ISSUE: Router Gap Observation False Positive (2026-02-22)

**Status**: RESOLVED — Gap observation inaccurate

**Observed Gap**: Router flagged task-27-research as "researcher produced TEST_STUB instead of actual research report for webmcp/Claude features"

**Reality**: `.claude/context/artifacts/research-reports/claude-features-webmcp-research-2026-02-22.md` is a **complete 200+ line report** with:

- Executive summary on 4 Claude features
- Research methodology (6 queries documented)
- 9 sources with credibility ratings
- Detailed findings on WebMCP (W3C proposal), Claude Memory Tool (84% token reduction), Worktrees (git isolation), Healthcare (FHIR integration)
- Integration opportunities for agent-studio

**Assessment**: FALSE POSITIVE — router's gap observation mechanism is unreliable

**Root Cause**: Router likely checked file metadata (size/modification time) rather than content verification

---

## ISSUE: Router Violated Tool Lockdown — Direct Bash Execution (2026-02-22 CRITICAL)

**Status**: OBSERVED — Router ran 9 Bash commands directly to debug reflection-agent issues instead of spawning developer

**Violation**: CLAUDE.md Section 0 (ROUTER TOOL LOCKDOWN) — Router is FORBIDDEN from running Bash directly except read-only git commands

**Evidence**: Session context from Router shows direct investigation instead of spawning qa/developer agent with debugging skill

**Impact**:

- Router became troubleshooter instead of orchestrator (violates separation of concerns)
- Task #38 left stale (`in_progress` after work completed) due to manual router investigation
- Undermines specialist-first routing law — troubleshooting should use qa or troubleshooting-regression skill
- Sets precedent for future rule violations

**Mitigation**:

- Enforce CLAUDE.md Section 0 via pre-completion-validation
- When Router detects agent issues, spawn qa agent with `troubleshooting-regression` skill INSTEAD of running Bash
- Add explicit audit rule: "If Router runs Bash (except git status/log), mark session AUDIT_REQUIRED"

---

## ISSUE: Task #38 Left Stale (in_progress after completion) (2026-02-22)

**Status**: UNRESOLVED — TaskUpdate metadata contract violated

**Observed**: Router completed work on task #38 but did not call TaskUpdate to mark it completed or update metadata

**Impact**:

- Task appears "stuck" in task list
- Blocks downstream tasks that depend on task #38
- Reflection spawn includes task #38 but without proper completion context
- Memory state inconsistent (work done, task status not updated)

**Root Cause**: Router did not follow mandatory task tracking protocol (CLAUDE.md Section 5.5-5.6):

- FIRST: `TaskUpdate(in_progress)`
- LAST: `TaskUpdate(completed)` with metadata
- Then: `TaskList()` to check for unblocked work

**Fix**: Manual cleanup — update task #38 metadata before closing session

**Learning**: Router gap observations need content validation before recording as systemic issues. File existence and size are insufficient signals for placeholder detection.

**Mitigation**: When router flags placeholder output, require reflection-agent to read file content before classifying as blocked/incomplete.

---

## ISSUE: Router Misrouting Precedent (2026-02-22) — Systemic Pattern

**Status**: OPEN — P2 (recurring risk, not blocking)

**Observed**: Developer agent used for git commit+push instead of devops (task-26)

**Expected**: CLAUDE.md Section 1 Common Misrouting table clearly documents: "git push / commit / deploy" → **devops specialist**

**Pattern**: This is a recurring misrouting risk:

- ✗ developer for deploy/CI/git operations
- ✓ devops for git push, commit, deployment, infrastructure

**Systematic Cause**: Router's specialist-first check (routing-guard.cjs Check 7) may not be catching all git/deploy intent variants

**Precedent**: Router successfully detected and logged this; reinforces importance of specialist routing enforcement

**Fix**: Expand routing keywords for devops agent to catch more git/CI/deployment intent variations

---

## ISSUE: Reflection-Agent Background Spawn Limitation (2026-02-22)

**Status**: OPEN — P1 mitigation required

**Root Cause**: Reflection-agent spawned with `run_in_background: true` loses TaskUpdate tool in whitelist

**Evidence**:

- From session-gap-log: "Root cause: run_in_background spawns may not receive full tool whitelist"

---

## ISSUE: Skill System Overhaul Integration Gaps (2026-03-03) — P0 BLOCKING

**Status**: OPEN — post-pipeline validation required before completion

**Context**: Tasks 27-31 completed multi-phase skill system overhaul (Phases 3-5). Framework infrastructure changes require bidirectional validation: forward (creation) + backward (registration verification). Backward pass missing.

**Gaps Identified**:

- [ ] Artifact-integrator post-pipeline validation not completed (Task 28 skill creation unknown)
- [ ] CLAUDE.md Section 8.5 skill roster updates not confirmed
- [ ] Artifact-graph edges not verified for new skill dependencies
- [ ] Skill-agent consistency (Step 4.7) not executed post-pipeline
- [ ] Proactive-audit (Task 30) output location not documented
- [ ] Integration health score (ADR-100) not recorded

**Root Cause**: Creator ecosystem requires post-creation integration audit (artifact-integrator), but this was not spawned as final pipeline step.

**Impact**: Orphaned skills, missing agent assignments, invisible artifacts may exist post-overhaul.

**Solution**: Spawn artifact-integrator immediately with skill names from Task 28-29 to validate: (1) catalog presence, (2) index presence, (3) agent assignment, (4) orphan status. Document findings in this issue.

**Owner**: router (Step 0.5 integration queue) or developer (manual remediation)

---

## ISSUE: Self-Healing Loop Evidence Gap (2026-03-02) — CRITICAL

**Status**: OPEN — P0 blocker for self-improvement maturity

**Observed**: Evolution-Reflection cycle operationally closed but evidentially broken. System executes phases (Reflection → Evolution → Creation → Validation) but **does not feedback outcome signals back into reflection context**.

**Evidence**:

- Task #4 synthesis identified 4 P0 fixes with no automated follow-up
- Reflection-log.jsonl contains recommendations, but no outcome field
- Creator outputs (skills, agents) don't include validation results
- Next reflection cycle has no visibility into whether prior recommendations succeeded

**Impact**:

- Duplicate patterns re-discovered in consecutive sessions (no staleness decay)
- Same evolution recommendations repeated (no outcome tracking)
- Self-healing loop appears broken because feedback is missing, not because of execution failure

**Gold-Standard Property Missing**: Outcome signal injection to close evidence loop

**Mitigation**:

1. Add `outcome` field to reflection-log.jsonl entries
2. Modify post-completion-chain.cjs to inject outcome signals when evolution-triggered changes complete
3. Wire creator validation results back to reflection spawn context
4. Implement dead-letter governance for failed creator attempts

**Target Fix Date**: 2026-03-03

---

## ISSUE: 2 Unregistered Hooks (2026-03-02) — P0 CRITICAL

**Status**: OPEN — Blocks reflection handshake

**Identified Hooks**:

1. **reflection-cleanup.cjs** — Should run PostToolUse(TaskUpdate) to atomically remove processed reflection requests from reflection-spawn-request.json
2. **[Hook #2 TBD]** — Pending full artifact audit

**Impact**:

- Without registration, reflection handshake signal (processedReflectionIds) is ignored
- Reflection requests accumulate as stale entries in spawn queue
- Session resets compound stale requests (50+ queue buildup observed)

**Evidence**: Task #1 component analysis found both hooks missing from settings.json

**Remediation**:

```bash
# 1. Add to .claude/settings.json
"reflection-cleanup.cjs": { "event": "PostToolUse:TaskUpdate", "priority": 100 }

# 2. Validate
node .claude/tools/cli/validate-hook-registration.cjs

# 3. Test
pnpm test .claude/hooks/reflection/reflection-cleanup.cjs
```

**Target Fix Date**: 2026-03-02 (immediate)

---

## ISSUE: Reflection Data Quality Gate Incomplete (2026-03-02)

**Status**: OPEN — P1 (impacts reflection reliability)

**Observed**: Phase 0 data sufficiency check (from reflection-agent.md) is correctly specified but not fully implemented in runtime

**Gap**:

- Agent definition documents Phase 0 (check summary, filesModified, dataQuality flag)
- But agent does not auto-remediate insufficient data
- Instead, score is withheld but no automatic follow-up triggered

**Example**: If task completes without summary, reflection-agent should auto-spawn a correction request asking the original agent to provide metadata

**Recommendation**:

- When dataQuality is "insufficient", auto-create TaskUpdate request to original agent asking for metadata completion
- Retry reflection after metadata provided

**Priority**: P1 (improves reflection reliability without blocking current workflows)

- Mitigation documented: "never spawn reflection-agent with run_in_background: true — always foreground"

**Pattern**: Applies to any background-spawned agent that needs atomic completion handshake

**Mitigation**: CLAUDE.md Step 0 must enforce reflection-agent ALWAYS foreground (no background spawns)

**Related**: May affect other background spawn patterns; check if run_in_background affects tool whitelist globally 2. Tool whitelist configuration missing TaskUpdate for reflection-agent 3. Skill framework overrides standard tool availability

**Workaround**: None — requires Router or system configuration fix.

---

## ISSUE: Worktree Isolation Breaks Code Review on Uncommitted Changes (2026-03-03) — Systemic

**Status**: OPEN — P1 (blocks core code-review capability)

**Root Cause**: Git worktree isolation creates isolated clone from clean HEAD. Code-reviewer agent spawned with `isolation: worktree` cannot see unstaged/uncommitted changes in parent working tree.

**Observed**:

- Task 1 (2026-03-03 04:04:47Z): code-reviewer + worktree isolation → cannot locate unstaged code
- Router detected and re-spawned without isolation → succeeded
- Gap log entry: "code-reviewer in worktree could not see unstaged working-tree changes"

**Systemic Pattern**: Worktree isolation is **incompatible with all in-flight code-analysis tasks**:

- code-reviewer (analyze uncommitted changes)
- architect (in-flight design review)
- code-simplifier (analyze current state)

**Current State**:

- code-reviewer.md has `isolation: worktree` in frontmatter
- This is the DEFAULT for this agent
- Results in systematic failures on in-flight review requests

**Impact**:

- BLOCKS: Any code-review or architecture-review task on uncommitted changes
- TRIGGERS: When task requires analyzing working-tree state + agent has worktree isolation enabled
- MITIGATION: Manually spawn without isolation (Router re-spawns on detection, but still wastes cycle)

**Fix Path** (P1):

1. Remove `isolation: worktree` from code-reviewer frontmatter (set to `isolation: none`)
2. Document limitation in CLAUDE.md routing section
3. Add spawn-time override for future conditional isolation (tasks could request isolation if needed)

**Evidence Files**:

- Gap log: `.claude/context/runtime/session-gap-log.jsonl` (line 1)
- Reflection report: `.claude/context/reports/reflections/lint-pipeline-reflection-2026-03-03.md`
- Task metadata: task-1 completed with retry (code-reviewer first failure, then success)

**Evidence**:

- Reflection report: `.claude/context/reports/reflections/reflection-tasks-21-22-14-insufficient-data-2026-02-22.md`
- Briefing requirement: "ATOMIC COMPLETION: In your final TaskUpdate({ status: "completed" }), include: metadata: { processedReflectionIds: [...] }"
- Error message: "No such tool available: TaskUpdate"

**Resolution Required**:

1. Check if reflection-agent was spawned correctly (should be Task(), not Skill())
2. Verify tool whitelist includes TaskUpdate for reflection-agent type
3. Re-invoke reflection-agent with correct spawning mechanism
4. Manually update reflection-spawn-request.json entries to mark processed: true if automated cleanup cannot run

**Priority**: P1 (blocks reflection completion handshake across entire system)

---

## ISSUE: Worktree-Isolated Agent No Inline Summary (2026-03-03 REFLECTION)

**Status**: OPEN — P1 (recurring, 3+ confirmed instances)

**Observed**: Agents running with `isolation: worktree` (developer, devops, qa) produce no inline text summary before calling TaskUpdate(completed). Router has zero visibility into what was accomplished without manually checking `git status -s` or `git diff HEAD~1`.

**Instances**:

- 2026-03-03 task-36: developer worktree → no files created at all (router rerouted to devops)
- 2026-03-03 Task 1: devops worktree → completed, no inline summary (router checked git status manually)
- 2026-03-03 Task 4: developer worktree → completed, no inline summary (router checked git diff manually)

**Root Cause**: Worktree-isolated agents write all output to isolated directory. No spawn prompt contract requires inline summary. Agents consider TaskUpdate(completed) alone sufficient.

**Impact**: Router cannot verify work was done; must add git-based manual verification for every worktree agent, increasing overhead and error risk.

**Fix Path** (P1):

1. Add mandatory inline summary requirement to worktree agent spawn prompts: "Before calling TaskUpdate(completed), output a 5-bullet inline summary of what was accomplished."
2. OR: post-TaskUpdate hook reads git diff in worktree and appends to task metadata automatically
3. Affects: developer, qa, code-reviewer, frontend-pro, nextjs-pro frontmatter + spawn template

**Evidence**:

- Gap log: `.claude/context/runtime/session-gap-log.jsonl`
- Reflection report: `.claude/context/reports/reflections/reflection-batch-session-2-2026-03-03.md`

---

## ISSUE: Worktree Accumulation on Missing TaskUpdate (2026-03-03 REFLECTION)

**Status**: OPEN — P2 (reliability risk)

**Observed**: `worktree-auto-cleanup.cjs` fires on `PostToolUse(TaskUpdate)` only. If an agent exits without calling TaskUpdate (error, timeout, context limit), its worktree persists indefinitely. 10 stale worktrees accumulated in one session.

**Root Cause**: Single cleanup trigger path (TaskUpdate) does not cover agent exit-without-update cases.

**Fix Path** (P2):

1. Add secondary cleanup: session-end hook or periodic cron in `.claude/tools/cli/`
2. Add worktree age-based pruning (> 2 hours old, task completed/deleted = safe to prune)
3. Reference: `worktree-auto-cleanup.cjs` in `.claude/hooks/`

**Evidence**:

- Gap log entry: task-36, 2026-03-03T08:30:00Z
- Reflection report: `.claude/context/reports/reflections/reflection-batch-session-2-2026-03-03.md`

---

## ISSUE: Multi-LLM Council False Positives — No Code Cross-Check Step (2026-03-03 REFLECTION)

**Status**: OPEN — P2 (quality risk)

**Observed**: Codex model flagged `bytesRead` as "potentially undeclared" (finding H2) in council review. Architect investigation revealed bytesRead IS declared at line 43 of the file. The false positive added noise and required architect time to resolve.

**Root Cause**: Multi-LLM council workflow does not include a mandatory cross-check step: "before including a HIGH/CRITICAL finding in the report, verify the claim against the actual code."

**Fix Path** (P2):

1. Add to council workflow instructions: "For each HIGH or CRITICAL finding, run a targeted code search to verify before including. Example: `rg -F 'variable_name' path/to/file` to confirm declared/undeclared status."
2. Add confidence level field to council findings (LOW/MEDIUM/HIGH confidence in the finding itself)
3. Reference: ADR-2026-03-03-108 (resolved this false positive)

**Evidence**:

- decisions.md: ADR-2026-03-03-108
- Reflection report: `.claude/context/reports/reflections/reflection-batch-session-2-2026-03-03.md`

---

## REFLECTION GATE FAILURE: Missing TaskUpdate Metadata Pattern (2026-03-04)

**Status**: ACTIVE — Pattern detected in 2 reflections

**Pattern**: Tasks completing without calling `TaskUpdate(completed)` with required metadata fields:

- No `summary` field (min 50 chars describing work)
- No `filesModified[]` array (list of changed files)
- No `outputArtifacts[]` array (list of created/modified artifacts)

**Impact on Reflection**:

- PHASE 0 data sufficiency gate withholds scores
- No evaluation possible without summary metadata
- Reflection reports cannot be generated
- Downstream integration health checks cannot run

**Evidence**:

- Task 6: Completed 2026-03-04T14:26:56.309Z without metadata
- Task 7: Completed 2026-03-04T14:26:56.319Z without metadata

**Root Cause**: Pre-completion-validation.cjs hook not blocking TaskUpdate calls missing required metadata fields.

**Fix**: Enforce metadata contract at TaskUpdate PreToolUse gate:

- Require `summary` with min length 50 chars
- Require `filesModified[]` non-empty (or explicitly empty list)
- Require `outputArtifacts[]` for substantial completions

**Priority**: P1 — blocks all reflection analysis

---

## SYSTEMIC ISSUE: Developer Worktree Placeholder Output — 43% Failure Rate (2026-03-04)

**Status**: RESOLVED (2026-03-04) — Fixed in commit 775ccf1f. See ADR-2026-03-04-064. `shouldOverrideWorktreeIsolation()` added to spawn-prompt-assembler.task-tools.cjs; framework paths now bypass worktree isolation automatically. 26 tests added.

**Pattern**: Tasks 3, 4, and 4-wave2-dev all produced ONLY `.claude/context/code-index/` changes with zero source code modifications.

**Root Cause**: Developer agent has `isolation: worktree` in frontmatter (documented in developer.md). When developer is spawned to work on framework paths (`.claude/hooks/`, `.claude/tools/`, `.claude/skills/`), the write target is the worktree clone (clean HEAD), not the parent repo. Changes to framework paths go into the clone and are discarded on worktree cleanup. Meanwhile, the code-indexer running in the worktree DOES write to `.claude/context/code-index/` (relative path), so index changes persist while source changes disappear.

**Instances in Current Session**:

1. Task 3 (2026-03-04T04:11:00Z): P0 LTM eviction fixes, worktree `ae8bf3c0` → only `.claude/context/code-index/` changed
2. Task 4 (2026-03-04T04:12:55Z): access_count wiring, worktree `a9ae5f1a` → only `.claude/context/code-index/` changed
3. Task 4-wave2-dev: stale-index detection, worktree `a51602f1` → only `.claude/context/code-index/` changed

**Prior Related Instance**: Code-reviewer with worktree isolation also failed (2026-03-03 morning, documented in earlier issue). Reroute without isolation → succeeded.

**Impact**: 43% of tasks in current session failed with placeholder output. Work is silently lost unless gap log detects it.

**Fix Options**:

**Option A (Recommended)**: Route framework tasks to non-isolated agents

- Tasks targeting `.claude/hooks/`, `.claude/tools/`, `.claude/agents/`, `.claude/skills/` → route to devops or technical-writer
- Keeps developer isolated for source code work only
- Technical-writer is designed for `.claude/` documentation paths

**Option B**: Spawn-time isolation override

- When developer task targets `.claude/` paths, override `isolation: none` at spawn time
- Simpler to implement but doesn't solve for code-reviewer

**Option C**: Completion validation gate (supplementary)

- Add to developer.md prompt: "Before marking complete, verify source files (not just .claude/context/code-index/) were modified"
- Gap log would catch this earlier
- Doesn't prevent the failure, just detects it sooner

**Recommendation**: Implement Option A + Option C. Route framework tasks out of developer worktree. Add completion validation gate as backstop.

**Evidence**:

- Gap log entries: 4 entries in session-gap-log.jsonl documenting placeholder output
- Batch reflection report: `.claude/context/reports/reflections/batch-reflection-session-2026-03-04.md`

**Priority**: P1 — blocks critical framework maintenance tasks

---

## SYSTEMIC ISSUE: Devops Commit Failures — Strike 2 Repetition (2026-03-04)

**Status**: OPEN — P1 (agent learned nothing from Strike 1)

**Instance**: Task 7 (2026-03-04T04:31:49Z) — "Commit all changes and push to remote"

**Symptom**: `git status` shows A/M staged files, but HEAD unchanged at 180009b2 after devops completed. Commit never succeeded despite files being staged.

**Root Cause Analysis** (requires investigation):

1. **Pre-commit hook blocking** (likely): ESLint rule SEC-023 (max-lines 500) may reject commit if any staged file exceeds limit
2. **Worktree isolation** (if task 7 spawned with isolation): Commit succeeds in clone but parent HEAD unchanged
3. **Error message suppression**: Devops spawn prompt may not propagate pre-commit hook output, so agent can't learn what's blocking

**Strike Pattern**: Gap log explicitly marks this as "Strike 2", implying Strike 1 also failed on same task. Agent did NOT learn from Strike 1 failure. Either:

- Same error repeated (hook is still blocking), OR
- Error message not fed back to agent, so retry is identical to first attempt

**Impact**: Work staged but uncommitted. Next session sees dangling staged changes. Remote push fails. Pipeline blocked.

**Verification Commands**:

```bash
# Check current state
git status -s
git log --oneline -5

# Check if worktree isolation was used (look for worktree/ in git branch name)
git branch -a

# Check pre-commit hook output (run manually)
git commit -m "test" 2>&1 | head -20  # Will fail but shows error
```

**Fix Recommendations**:

1. **Immediate** (this session): Verify task 7 git state. Are files still staged or committed?

2. **Root cause investigation**:
   - Check if devops agent has `isolation: worktree` (it shouldn't — devops needs to commit to parent)
   - Run pre-commit hook manually: `git commit --all -m "test"` and capture error output
   - Check if ESLint max-lines is blocking any staged files

3. **Error context propagation**: Devops spawn prompt should include pre-commit hook output when retry is attempted. Currently error message is lost.

4. **Strike 3 gate**: After Strike 2 failure, instead of automatic Strike 3 retry, ask user for input (per 3-strike-rule in CLAUDE.md Section 0.1).

**Related gotcha**: "Devops commit pattern (SYSTEMIC)" in codebase map. This issue is known to recur.

**Priority**: P1 — blocks release/deployment pipeline

---

## ISSUE: Multi-LLM Consultant Agent Missing TaskUpdate Metadata (2026-03-05)

**Status**: OPEN — P1

**Summary**: Task 13 (multi-llm-consultant dedup review) completed with fallback summary "Task 13 completed without summary metadata". This extends the known metadata failure pattern from devops agent to multi-llm-consultant agent. Now confirmed across at least 2 distinct agent types.

**Cumulative count**: 16th-17th documented violations in current session group. See prior P1 escalation entries below.

**Pattern**: Review/commit pipeline tail agents (last agents in a multi-agent pipeline) are the highest-risk agents for missing TaskUpdate metadata. They complete after the main implementation and receive less router attention.

**Required action**: Add metadata enforcement warning to multi-llm-consultant agent spawn template. Consider metadata check after all pipeline phases complete.

---

## ISSUE: Reflection Atomic Handshake Failure — Spawn Request Not Cleared (2026-03-05)

**Status**: OPEN — P2

**Summary**: Reflection-agent completed full analysis (reflection-log.jsonl entry, report file written) for batch tasks 8-12 at 2026-03-05T15:00:00Z but did not submit processedReflectionIds in TaskUpdate metadata. The atomic handshake contract requires TaskUpdate({ status: 'completed', metadata: { processedReflectionIds: [...] } }) to trigger reflection-cleanup.cjs. Without this, reflection-spawn-request.json retains the IDs and Router re-spawns reflection-agent on every subsequent prompt.

**Evidence**: reflection-log.jsonl has batch-8-12 entry at 2026-03-05T15:00:00Z; reflection-spawn-request.json still contains 5 IDs from that batch as of 2026-03-05T18:27Z.

**Root Cause**: reflection-agent completed analysis but either (a) crashed before TaskUpdate, (b) TaskUpdate call was blocked, or (c) processedReflectionIds was omitted from metadata.

**Prevention**: reflection-cleanup.cjs should validate that TaskUpdate includes non-empty processedReflectionIds before removing spawn request entries. Add verification in reflection-agent completion protocol: always check spawn request is cleared after TaskUpdate(completed).

**Resolution**: This batch re-processed with handshake submission on 2026-03-05T18:27Z.

---

## ISSUE: Router Gate 4 Violation — Direct Edit to lsp-navigator/SKILL.md (2026-03-06)

**Status**: OPEN — P1 (IRON LAW BREACH)

**Context**: Router directly used Edit tool on `.claude/skills/lsp-navigator/SKILL.md` when user requested "update the skill". This violates Gate 4 (Creator Workflow Iron Law): SKILL.md files must only be modified via `skill-updater` skill, never directly.

**Required path**: User asks "update skill" → Router spawns developer/nodejs-pro agent → agent invokes `Skill({ skill: 'skill-updater' })` → skill-updater makes controlled edits.

**What happened**: Router bypassed this workflow, editing SKILL.md directly. If `unified-creator-guard.cjs` was active in block mode, this should have been prevented — the fact that it succeeded suggests CREATOR_GUARD was in warn/off mode or the hook was not triggered.

**Gap Log Entry**: Recorded at 2026-03-06T23:30:00Z in `session-gap-log.jsonl`.

**Fix**:

1. Reinforce "update the skill" routing pattern in router's decision logic
2. Verify `CREATOR_GUARD` env var is set to block (default)
3. Verify `unified-creator-guard.cjs` hooks for Edit tool on `skills/**` paths

**Source**: reflection of session 2026-03-06 (gap log analysis)

---

## ISSUE: Hook Exit Code 1 in user-prompt-unified.cjs — SE-03 Violation (2026-03-06)

**Status**: OPEN — P2

**Context**: Debug log shows `user-prompt-unified.cjs` firing with exit code 1 at session start (22:56:49.156Z). Per SE-03 (Sharp Edges), exit code 1 is treated as an ERROR, not a BLOCK. Only exit code 2 produces a block.

**Evidence**: `{"block":true,"message":"Blocked by .claude/hooks/routing/user-prompt-unified.cjs (non-zero exit code 1)"}`

**Impact**: The hook intended to block may have allowed through content it should have blocked. The `block:true` in the JSON response overrode this, but the hook's own exit code was wrong.

**Fix**: Audit `user-prompt-unified.cjs` error paths to ensure they exit 2 (block) not 1 (error) when a block condition is detected.

**Source**: reflection of session 2026-03-06 (debug log line 473-478)

---

## ISSUE: Agent Isolation Values Invalid — general-assistant and multi-llm-consultant (2026-03-06)

**Status**: OPEN — P2

**Context**:

- `general-assistant.md`: `isolation: none` — not a valid Claude Code value (only `worktree` is valid)
- `multi-llm-consultant.md`: `isolation: standard` — not a valid Claude Code value

**Effect**: Both agents silently fail isolation validation at startup. The debug log warns: "invalid isolation value 'none'. Valid options: worktree"

**Fix**:

- `general-assistant.md`: remove the `isolation:` field entirely (omitting = no worktree isolation, which is the desired behavior)
- `multi-llm-consultant.md`: remove the `isolation:` field entirely

**Source**: reflection of session 2026-03-06 (debug log lines 171-172)

---

## ISSUE: Devops Commit Verification — No Escalation Path After Retry Failure (2026-03-06)

**Status**: OPEN — P2

**Context**: Task 3 added a Commit Verification Protocol to devops.md requiring pre/post HEAD hash comparison. The protocol defines what to do when a commit is detected as failed (retry with diagnosis), but does not define an escalation path when the retry also fails.

**Gap**: The "3-strike" failure escalation rule from CLAUDE.md (Strike 1: retry, Strike 2: re-route to different specialist, Strike 3: AskUserQuestion) is not referenced in the Commit Verification Protocol. An agent following the protocol strictly would retry indefinitely without knowing when to escalate.

**Fix**: Update devops.md Commit Verification Protocol to reference the 3-strike escalation rule. After 2 failed commits: escalate to devops-troubleshooter or AskUserQuestion with commit error context.

**Priority**: P2 — protocol gap, not blocking (base protocol is working correctly)

**Source**: reflection of task 3 (reflection-batch-2, 2026-03-06)

---

## ISSUE: Committing With Failing Tests — 47 agent-search-tool-compliance Failures (2026-03-07)

**Status**: OPEN — P2

**Context**: Commit e5e37986 ("chore: sync agent registry timestamps + regenerated config indexes") was made while 47 tests in `tests/agents/agent-search-tool-compliance.test.cjs` were failing. The summary correctly labeled these as "pre-existing" failures, but they were not resolved or tracked before committing.

**Pattern**: The test file validates that specific agents have required search skills (ripgrep, code-semantic-search, code-structural-search, token-saver-context-compression) in the agent-skill-matrix.json. The failures indicate matrix entries are missing for agents in Phase 1/2/3 groups.

**Impact**: CI state is degraded. 47 failing tests obscure new regressions introduced in future commits. The `agent-skill-matrix.json` and `agent-registry.json` are out of sync with what the compliance tests require.

**Fix Required**:

1. Run `node --test tests/agents/agent-search-tool-compliance.test.cjs` to see exact failing agents
2. Update `.claude/config/agent-skill-matrix.json` to add missing search skills for failing agents
3. Regenerate `agent-registry.json` with `pnpm agents:registry`
4. Verify all tests pass before next commit

**Priority**: P2 — degrades CI signal quality; blocks clean test baseline

**Source**: reflection of task_completion:2026-03-07T19:20:39.233Z

---

## ISSUE: TaskUpdate Metadata Compliance — Task 13 Missing Summary (2026-03-07, recurrence 15+)

**Status**: OPEN — P0 (ESCALATED — requires immediate developer implementation of block mode)

**Instance**: Task 13 (2026-03-07T19:29:38.520Z) — completed with fallback text "Task 13 completed without summary metadata"

**Pattern**: Identical fallback string. Recurrence counter estimated at 15+ based on reflection-log.jsonl sequence (count=13 at task 9, count=14 at task 6, now task 13).

**Data Quality**: Reflection analysis yielded `dataQuality: "insufficient"` — score withheld per Iron Law. No filesModified, no outputArtifacts, no summary provided.

**Escalation Status**: The P1 issues recorded on 2026-03-05 called for converting `pre-completion-validation.cjs` from advisory to blocking. As of 2026-03-07, that change has NOT been implemented. Task 13 is the 15th+ occurrence confirming the fix has not been deployed.

**Required Action**: Developer agent MUST update `pre-completion-validation.cjs` to BLOCK (exit code 2) when summary matches the fallback pattern `/Task \d+ completed without summary metadata/`. Advisory mode has failed across 15+ task violations.

**Priority**: P0 — systemic reflection quality degradation; each missing-metadata completion wastes a full reflection cycle.

**Source**: reflection of task 13 (2026-03-07)

---

## ISSUE: Duplicate Reflection Spawning — Same Commit Reflected Twice (2026-03-07)

**Status**: OPEN — P3

**Context**: Tasks 1 and reflection-task-completion-2026-03-07t19-20-39-233z both scored commit e5e37986 (registry timestamp sync). Both reflections fired independently, produced separate log entries, and issued separate atomic handshakes. No deduplication occurred.

**Impact**: Wasted reflection capacity (2 agents spawned for same commit). Report file was likely overwritten. Patterns recorded twice.

**Fix**: Add deduplication check in `reflection-spawn-request.json` processor — before spawning reflection-agent, check if a reflection entry with the same `taskId` already exists in `reflection-log.jsonl`. If yes, skip re-spawning.

**Priority**: P3 — capacity waste, not correctness failure

**Source**: reflection batch 2026-03-07

---

## ISSUE: TaskUpdate Metadata Missing — 2026-03-07 Session Batch (Recurrence Count: 8+) (2026-03-07)

**Status**: OPEN — P1 (CRITICAL THRESHOLD EXCEEDED)

**Instances this batch** (5 of 5 tasks failed or had partial data):

- Task 13 (2026-03-07T19:29:38Z) — fallback summary, score withheld
- Task 1 (2026-03-07T19:43:33Z) — real summary (from prior reflection), memoryWrites empty
- Task 2 (2026-03-07T19:46:27Z) — fallback summary, score withheld (partial from git context)
- Task 7 (2026-03-07T20:02:19Z) — fallback summary, score withheld (partial from git context)

**Cumulative pattern**: Recurrence count now 8+ across multiple sessions. The `pre-completion-validation.cjs` advisory mode has demonstrably failed to enforce the contract.

**Root cause**: Agents complete tasks without calling `TaskUpdate(completed, { summary: "...", filesModified: [...] })`. The post-completion-chain.cjs fires the reflection hook but has nothing to extract.

**Required Action**:

1. Convert `pre-completion-validation.cjs` from `warn` to `block` mode for fallback summary pattern
2. All spawn templates MUST include explicit summary contract: "summary field MUST be 50+ chars describing what was accomplished"
3. Escalate to `evolution-orchestrator` if next 3 reflections still yield insufficient data

**Priority**: P1 — systemic learning system degradation

**Source**: reflection batch 2026-03-07 (requests 1, 2, 4, 5)

---

## ISSUE: agent-search-tool-compliance.test.cjs — 47 Tests Failing at Commit Time (2026-03-07)

**Status**: OPEN — P2

**Context**: Commit e5e37986 (registry timestamp sync) was merged with 47 failing tests in `agent-search-tool-compliance.test.cjs`. No remediation task was created at the time of the commit. Reflection scored this at 0.635 (WARNING).

**Impact**: Test suite is in a degraded state. CI would report failure on any test run. 47 tests represent systematic gap in agent search tool compliance.

**Fix**: Create dedicated QA task to investigate and fix the 47 failing tests. Run `node --test tests/lib/agents/agent-search-tool-compliance.test.cjs` to reproduce.

**Priority**: P2 — test suite degraded but non-blocking for feature work

**Source**: reflection of commit e5e37986, reported 2026-03-07

---

## ISSUE: TaskUpdate Metadata Compliance — Batch of 4 Tasks Missing Summary (2026-03-07 Session C)

**Status**: OPEN — P1 (recurrence continues)

**Instance**: Tasks 3, 4, 5, 6 all completed with fallback text "Task X completed without summary metadata".

**Context**: Tasks represent: (3) skill-creator SKILL.md post-creation fix, (4) routing/catalog doc updates, (5) telegram-polling skill creation + openclaw report, (6) openclaw assimilation research report. All 4 are substantive work — none are trivial.

**Impact**: Reflection analysis degraded to `dataQuality: "partial"` for all 4. Scores withheld due to insufficient primary metadata. Context provided via trigger metadata field allowed partial analysis, but no filesModified lists, no outputArtifacts paths confirmed from TaskUpdate.

**Pattern**: This is the Nth occurrence (14+ total). The `pre-completion-validation.cjs` block mode is active as of EPIC audit session, yet violations continue. Root cause may be agents completing via direct response rather than explicit TaskUpdate(completed, {...}).

**Recommendation**: Verify `SUMMARY_REQUIRED_ENFORCEMENT=block` is set in .env and that the hook matches the exact fallback string pattern for all 4 agents spawned in this session type.

**Source**: reflection of tasks 3, 4, 5, 6 (2026-03-07)

---

## ISSUE: Skill-Creator Post-Creation Workflow Stale References (2026-03-07)

**Status**: RESOLVED (commit 76ff12f3)

**Context**: skill-creator SKILL.md Steps 6 and 8 referenced:

- "Section 8.5" of CLAUDE.md (no longer exists in v3.0.0)
- `skill-catalog.md` (canonical is now `@SKILL_CATALOG_TABLE.md`)
- No step for updating `@AGENT_ROUTING_TABLE.md` when a skill introduces a new agent

**Fix**: commit 76ff12f3 updated Step 6 to reference v3.0.0 structure, Step 8 to use @SKILL_CATALOG_TABLE.md, added Step 8b for @AGENT_ROUTING_TABLE.md, and updated Step 9 verification checklist.

**Impact**: Prior to fix, newly-created skills that introduced agents/orchestrators would NOT be added to the Router's routing table, making them invisible.

**Lesson**: After any major CLAUDE.md rewrite (e.g. v2.x → v3.0.0), ALL creator skills' post-creation checklists must be audited for stale section references.

---

## ISSUE: TaskUpdate Metadata Compliance — Tasks 12 and 16 Missing Summary (2026-03-07 Session C, second batch)

**Status**: OPEN — P1 (ESCALATED — 15+ cumulative violations, now two in same day second batch)

**Instances**:

- Task 12 (2026-03-07T21:53:22.228Z) — fallback summary "Task 12 completed without summary metadata", score withheld
- Task 16 (2026-03-07T21:59:15.496Z) — fallback summary "Task 16 completed without summary metadata", score withheld

**Pattern**: Both tasks completed in the same session, both missing TaskUpdate summary. The `pre-completion-validation.cjs` advisory hook is not enforcing the metadata contract. Scores withheld per PHASE 0 Data Sufficiency Gate (Iron Law: never fabricate a score).

**Gap count**: This represents the 15th+ violation. Previous instances documented in this file. `pre-completion-validation.cjs` must be converted to blocking mode for the fallback string pattern.

**Router gap log**: `session-gap-log.jsonl` was absent — router did not log a `missing_metadata` gap entry for these tasks. Gap logging must be enforced when agents complete without summary metadata.

**Source**: reflection of tasks 12 and 16 (reflection batch 2026-03-07T21:59)

---

## ISSUE: TaskUpdate Metadata Compliance — Task 12 Missing Summary (2026-03-07)

**Status**: OPEN — P1 (ESCALATED — recurrence count now 15+, pattern persists across sessions)

**Instance**: Task 12 (2026-03-07T21:53:22.228Z) — completed with fallback text "Task 12 completed without summary metadata"

**Data Quality**: Reflection analysis yielded `dataQuality: "insufficient"` — score withheld per Iron Law. No filesModified, no outputArtifacts, no summary provided.

**Cumulative pattern**: This is violation 15+ in an ongoing systemic failure. The `pre-completion-validation.cjs` hook remains in advisory/warn mode despite documented P1 escalation. The blocking upgrade (changing `/Task \d+ completed without summary metadata/` detection from warn to block) has NOT yet been implemented.

**Gap log**: No session-gap-log.jsonl existed for this session (file not found). The router did not log a `missing_metadata` gap entry for Task 12.

**Required Actions** (previously documented, still unimplemented):

1. Update `pre-completion-validation.cjs`: change summary validation from warn to BLOCK when summary matches `/Task \d+ completed without summary metadata/`
2. Update all agent spawn templates to include the exact summary contract
3. Track gap log misses: router must emit `missing_metadata` gap entries when TaskUpdate(completed) lacks a real summary

**Source**: reflection-task-completion-2026-03-07t21-53-22-228z

---

## ISSUE: Pre-Existing Test Failures — Task 22 Verification (2026-03-08)

**Status**: OPEN — P2

**Context**: Task 22 was a QA verification pass that reduced test failures from 18 to 13. The remaining 7 failures are confirmed pre-existing P2 issues (not introduced by recent work).

**Remaining Known Failures by Line/Test ID**:

- Lines 79, 80 — pre-existing, category unknown (no filesModified metadata)
- Line 155 — pre-existing
- Line 232 — pre-existing
- Line 481 — P2 priority, pre-existing
- Line 552 — pre-existing
- Line 721 — pre-existing

**Fixed in Task 22**: Lines 70, 113, 137-139, 146, 170, 236, 428, 542-546, 802 (~12 distinct fixes)

**Net Progress**: 18 → 13 failures. 5 remaining beyond the known pre-existing set may need a follow-up verification pass to confirm the exact test file(s) these refer to.

**Priority**: P2 — non-blocking, pre-existing issues

**Source**: reflection of task 22 (2026-03-08)

---

## ISSUE: TaskUpdate Metadata Missing — Task 42 (2026-03-08)

**Status**: OPEN — P1 (SYSTEMIC RECURRENCE)

**Context**: Task 42 completed with fallback summary "Task 42 completed without summary metadata". No `filesModified`, `outputArtifacts`, or meaningful `summary` provided in TaskUpdate metadata.

**Impact**: Reflection agent cannot score the task; data quality is "insufficient". Score withheld per Iron Law. Audit trail gap.

**Cumulative Pattern**: This is a recurring systemic issue. The reflection log shows the `missing_task_summary` failure class has been documented since at least 2026-02-14 with 14+ cumulative violations. `pre-completion-validation.cjs` advisory mode is demonstrably insufficient.

**Recommendation**: Escalate `pre-completion-validation.cjs` enforcement from advisory to `block` mode for agents that have repeatedly failed to provide summaries.

**Source**: reflection of task 42 (2026-03-08T04:47:42.165Z)

---

## ISSUE: TaskUpdate Metadata Compliance — Task 39 Missing Summary (2026-03-08)

**Status**: OPEN — P1 (RECURRING — now 15+ violations, session 2026-03-08)

**Instance**: Task 39 (2026-03-08T04:47:41.163Z) — completed with fallback text "Task 39 completed without summary metadata"

**Data Quality**: `dataQuality: "insufficient"` — score withheld per Iron Law. No `filesModified`, no `outputArtifacts`, no agent type provided.

**Pattern**: Same fallback string observed across Tasks 7, 8, 1, 4, 10, 9, 16, 1 (session 2), and now Task 39. The `pre-completion-validation.cjs` hook remains in advisory mode despite P1 escalation documented in prior reflection entries.

**Cumulative Impact**: Null-yield reflection event. Reflection cycle wasted (hook fired, agent spawned, no learnings extracted, no score produced). Audit trail for Task 39 is a complete blank.

**Required Action (UNCHANGED from prior issues)**: `pre-completion-validation.cjs` MUST be converted to BLOCK mode for the fallback pattern `/Task \d+ completed without summary metadata/`. This has been documented as P1 for 3+ sessions without resolution.

**Source**: reflection of task 39 (2026-03-08)

---

## ISSUE: TaskUpdate Metadata Compliance — Enterprise Workflow Session Batch Failure (2026-03-08)

**Status**: OPEN — P1 (PERSISTENT SYSTEMIC — 14+ cumulative violations)

**Session**: Enterprise Workflow Fix Session 2026-03-08

**Instances this batch** (all yielding fallback string "Task X completed without summary metadata"):

- Task 40 (2026-03-08T04:47:41.906Z) — no summary, no filesModified, no outputArtifacts
- Task 42 (2026-03-08T04:47:42.165Z) — no summary, no filesModified, no outputArtifacts
- Task 44 (2026-03-08T04:47:42.578Z) — no summary, no filesModified, no outputArtifacts
- Task 30 (2026-03-08T04:58:47.194Z) — no summary, no filesModified, no outputArtifacts

**Reflection result**: All 4 yielded `dataQuality: "insufficient"` — score withheld per Iron Law. 4 wasted reflection cycles.

**Note**: Task 31 (same session) DID provide a real summary ("Staged and committed .claude/config/skill-index.json and .claude/context/agent-registry.json"), confirming agents CAN provide summaries when they choose to.

**Failure-recurrence.json count**: 5 (as of session end, missing_task_summary violations in recurrence tracker)

**Required action (still pending)**:

1. Convert `pre-completion-validation.cjs` from advisory to BLOCK mode for fallback string pattern
2. All agent spawn templates must enforce summary contract (50+ chars, non-fallback)

**Priority**: P1 — no remediation implemented despite 3+ previous P1 escalations

---

## ISSUE: issues.md Cleanup — No Datestamp on Consolidated ROUTING WARN Note (2026-03-06)

**Status**: OPEN — P3

**Context**: Task 4 pruned ~200+ one-liner [ROUTING WARN] entries from issues.md and replaced them with a single consolidated summary note. The consolidation was well-executed (76% line reduction) but the consolidated note has no datestamp or count of entries replaced.

**Gap**: Future reviewers cannot determine when the consolidation happened or how many entries were replaced. The note is: "~200+ routing warnings logged from routing-guard.cjs test runs spanning 2026-02-24 through 2026-03-02" — the "spanning" range is correct but the date of consolidation is missing.

**Fix**: When performing bulk pruning of issues.md, always append "(consolidated YYYY-MM-DD, N entries replaced)" to the summary note.

**Priority**: P3 — audit trail gap, easily remediated

**Source**: reflection of task 4 (reflection-batch-2, 2026-03-06)

---

## ISSUE: TaskUpdate Metadata Compliance — Task 14 Fallback Summary (2026-03-08)

**Status**: OPEN — P1 (RECURRENCE — systemic failure continues unabated)

**Instance**: Task 14 (2026-03-08T06:38:23.711Z) — completed with fallback text "Task 14 completed without summary metadata"

**Data Quality**: Reflection analysis yielded `dataQuality: "insufficient"` — score withheld per Iron Law.

**Pattern**: Continuation of the same recurring P1 violation. Pattern now spans tasks 7, 8, 1, 4, 10, 9, 16, 20, 23, 24, 44, 14, and more. The `pre-completion-validation.cjs` hook remains in advisory (warn) mode and has failed to prevent this violation for 15+ consecutive occurrences.

**Recommended Action**: Convert `pre-completion-validation.cjs` summary check from warn to block. This P1 fix has been documented since 2026-03-05 without implementation.

**Source**: reflection of task 14 (reflection-task-completion-2026-03-08t06-38-23-711z)

---

## Skill Registration Gap: arxiv-monitor (2026-03-08)

- [ ] Catalog: PRESENT
- [ ] Index: PRESENT (agentPrimary: ["developer"], but no agent frontmatter lists this skill)
- [ ] Agent assignment: MISSING (no agent .md has arxiv-monitor in skills: array)
- [ ] Category drift: SKILL.md frontmatter says `category: research`, skill-index resolved to `Other` — CATEGORY_MAP does not include "research"
      Source: reflection of task 12

## Skill Registration Gap: exa-monitor (2026-03-08)

- [ ] Catalog: PRESENT
- [ ] Index: PRESENT (agentPrimary: ["developer"], but no agent frontmatter lists this skill)
- [ ] Agent assignment: MISSING (no agent .md has exa-monitor in skills: array)
- [ ] Category drift: SKILL.md frontmatter says `category: research`, skill-index resolved to `Other` — CATEGORY_MAP does not include "research"
      Source: reflection of task 12

---

## ISSUE: TaskUpdate Metadata Compliance — Stale Reflections Tasks 16 and 1 (2026-03-05, session 2)

**Status**: OPEN — P1 (ESCALATED FURTHER — two more null-yield reflections in this session batch)

**Instances this batch**:

- Task 16 (2026-03-05T04:46:28.321Z) — fallback summary, score withheld
- Task 1 (2026-03-05T05:30:30.891Z) — fallback summary, score withheld
- Task 2 (2026-03-05T05:33:56.031Z) — real summary provided, score 0.81 (PASS, dataQuality: partial)

**Note**: Task 2 had a real summary ("Updated orchestrator agent prompts for search-first compliance") demonstrating agents CAN provide summaries. Tasks 16 and 1 are strict enforcement failures.

**Cumulative pattern**: 14+ violations now documented. The `pre-completion-validation.cjs` advisory mode is demonstrably insufficient.

**Immediate action required**: Convert hook from advisory (warn) to blocking (block) for the fallback string pattern `/Task \d+ completed without summary metadata/`.

**Priority**: P1 — now requires developer agent to implement the block mode.

---

## ISSUE: TaskUpdate Metadata Compliance — Task 24 Missing Summary (2026-03-08, Batch 2)

**Status**: OPEN — P1 (RECURRING — planner agent failed to provide summary on EPIC plan task)

**Instance**: Task 24 (2026-03-08T08:39:57.321Z) — completed with fallback text or no summary. Planner created EPIC plan but did not include summary metadata.

**Data Quality**: Reflection analysis yielded `dataQuality: "insufficient"` — score withheld per Iron Law.

**Pattern**: Planner agents are now a documented offender type alongside developer agents. This is the second planner-related missing-metadata incident.

**Action**: Pre-completion-validation.cjs must block fallback summary string for ALL agent types including planner. The planner spawn template must be updated to explicitly require summary metadata.

---

## ISSUE: Integration Gap — heartbeat-sentinel.cjs Not in artifact-graph.json (2026-03-08)

**Status**: OPEN — P2

**Instance**: Tasks 25+28 (2026-03-08) created `.claude/lib/heartbeat/heartbeat-sentinel.cjs` and updated `heartbeat-orchestrator.md`. Neither artifact is registered in `.claude/context/data/artifact-graph.json` per ADR-100.

**Impact**: artifact-integrator cannot discover or validate this module. Router Step 0.5 heartbeat feature is operational but not cataloged.

**Required Fix**:

1. Add `heartbeat-sentinel.cjs` entry to `artifact-graph.json` with proper integration metadata
2. Update tool-catalog.md or relevant docs to reference this utility
3. Confirm `heartbeat-orchestrator.md` routing keywords are present in agent-registry.json

**Priority**: P2

**Source**: reflection of tasks 25 and 28 (batch-3, 2026-03-08)

---

## ISSUE: TaskUpdate Metadata Compliance — Tasks 17-20 Batch Missing Summary (2026-03-08)

**Status**: OPEN — P1 (RECURRENCE — systemic failure continues)

**Instance**: Tasks 17, 18, 19, 20 (2026-03-08T08:39:57-59Z) — all completed with no summary metadata. This is batch 3 of this EPIC session and the same missing-metadata pattern persists.

**Recurrence count**: Now 15+ instances documented (tasks 7, 8, 1, 4, 10, 9, 16, 20, 23, 24, 44, 15, 14, and now 17-20). 4 tasks in a single batch.

**Data Quality**: All 4 reflections yielded `dataQuality: "insufficient"` — scores withheld per Iron Law. Audit trail for these tasks is blank.

**Pattern**: Meta-reflection tasks (reflection-on-reflection) appear most likely to lack summary metadata, possibly because the reflection-agent itself does not always populate TaskUpdate with a meaningful summary field when the reflected task had no summary.

**Required Fix**: Same as prior instances — `pre-completion-validation.cjs` must be converted from advisory to blocking for the fallback pattern. Also: reflection-agent spawn template must include explicit instruction to populate summary even for insufficient-data tasks.

**Priority**: P1

**Source**: reflection-agent batch-3 (2026-03-08), reflection-task-completion IDs 2026-03-08T08:39:57/58/59Z

---

## ISSUE: TaskUpdate Metadata Compliance — Task 64 Fallback Summary + Stale Task (2026-03-08)

**Status**: OPEN — P1 (RECURRENCE — systemic failure continues)

**Instance**: Task 64 (2026-03-08T17:32:24.606Z) — completed with fallback text "Task 64 completed without summary metadata"

**Gap Log Entry**: `session-gap-log.jsonl` recorded `missing_metadata` for `task-lifecycle-42` — stale in_progress for 18 minutes without completion. This co-occurred with the Task 64 reflection trigger, suggesting pipeline stall pattern.

**Data Quality**: Reflection analysis yielded `dataQuality: "insufficient"` — score withheld per Iron Law.

**Recurrence**: This is the Nth instance (tasks 7, 8, 1, 4, 10, 9, 16, 20, 23, 24, 44, 15, 14, 64+). No additional pattern documentation needed — pattern is fully established.

**Source**: reflection of task 64 (reflection-task-completion-2026-03-08t17-32-24-606z)
