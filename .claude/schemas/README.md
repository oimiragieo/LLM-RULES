# Schemas

New schemas added for the Auto-Claude deep-dive integration:

- `.claude/schemas/agent-config.schema.json` — validates `agent-config.json`
- `.claude/schemas/phase-models.schema.json` — validates `phase-models.json`
- `.claude/schemas/implementation-plan.schema.json` — validates `implementation_plan.json`
