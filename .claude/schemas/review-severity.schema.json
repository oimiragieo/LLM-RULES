{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agent-studio.dev/schemas/review-severity.schema.json",
  "title": "Review Severity",
  "description": "Severity classification for code review findings. Four levels in descending order of urgency: blocker (release-blocking), critical (must fix before merge), suggestion (should fix, not blocking), nit (minor style or polish).",
  "type": "string",
  "enum": ["blocker", "critical", "suggestion", "nit"]
}
