/**
 * Default gate script
 * Usage:
 * export const gate = async (context) => {
 *   // Verify conditions
 *   return true; // or throw Error
 * };
 */

export const gate = async _context => {
  return true;
};
