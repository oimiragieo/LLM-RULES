#!/usr/bin/env node

/**
 * requesting-code-review companion tool
 * Dispatch code-reviewer agent for structured two-stage code review — prepares diff context, submits review request, and tracks feedback to resolution
 */

if (process.argv.includes('--help')) {
  console.log('requesting-code-review companion tool');
  process.exit(0);
}

console.error('Not implemented');
process.exit(1);
