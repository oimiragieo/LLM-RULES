#!/usr/bin/env node
'use strict';

/**
 * authentication-flow-rules tool - CLI entry point
 * Wraps the authentication-flow-rules skill for CLI invocation.
 * 
 * Usage: node .claude/tools/authentication-flow-rules/main.cjs [--json] [--help]
 */

const fs = require('fs');
const path = require('path');

function main() {
  const args = process.argv.slice(2);
  
  if (args.includes('--help')) {
    console.log('Usage: node ' + path.basename(__filename) + ' [--json] [--help]');
    console.log('Wraps the authentication-flow-rules skill for CLI invocation.');
    process.exit(0);
  }
  
  const jsonMode = args.includes('--json');
  
  // Load skill definition
  const skillPath = path.resolve(__dirname, '../../skills/authentication-flow-rules/SKILL.md');
  if (!fs.existsSync(skillPath)) {
    const err = { error: 'Skill SKILL.md not found at ' + skillPath };
    console.error(jsonMode ? JSON.stringify(err) : err.error);
    process.exit(1);
  }
  
  const result = {
    skill: 'authentication-flow-rules',
    status: 'ready',
    skillPath: skillPath,
    message: 'Skill authentication-flow-rules is available. Invoke via Skill({ skill: "authentication-flow-rules" }) in agent context.'
  };
  
  if (jsonMode) {
    console.log(JSON.stringify(result, null, 2));
  } else {
    console.log(result.message);
  }
}

if (require.main === module) {
  main();
}

module.exports = { main };
