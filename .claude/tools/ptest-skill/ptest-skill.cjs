#!/usr/bin/env node

/**
 * ptest-skill companion tool
 * A temporary test skill for verifying protocol injection
 */

if (process.argv.includes('--help')) {
  console.log('ptest-skill companion tool');
  process.exit(0);
}

console.error('Not implemented');
process.exit(1);
