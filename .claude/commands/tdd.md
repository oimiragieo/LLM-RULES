description: Canon TDD workflow (scenario backlog, RED proof, minimal GREEN, optional refactor)
disable-model-invocation: true

---

Invoke the `tdd` skill and execute Canon TDD:

1. Keep/update a scenario backlog.
2. Run one scenario per RED->GREEN cycle.
3. Capture RED and GREEN evidence.
4. Run final test/lint/format checks before completion.
