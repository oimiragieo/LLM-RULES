---
description: Show per-agent × per-tool token burn for a session with color-coded thresholds
---

Run `pnpm session:audit $ARGUMENTS` to display the token burn table for the specified session ID.

The table breaks down token consumption by agent, skill, and tool call using trace-recorder JSONL data. No external observability service required.

Usage: `/session-audit <session-id>`
